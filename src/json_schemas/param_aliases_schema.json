R"(
{
  "$id": "melbinst_param aliases_schema",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Params Aliases",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "param": {
        "type": "string",
        "description": "The path of the param to alias"
      },
      "alias": {
        "type": "string",
        "description": "The path of the alias"
      }
    },
    "required": [
        "param",
        "alias"
    ]
  }
}
)"